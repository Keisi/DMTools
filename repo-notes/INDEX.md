# DMTool-FrontEnd repo-notes — index (auto-loaded; keep lean)

Durable, non-obvious learnings about how DMTool-FrontEnd (and its boundary with the DMTool
backend API) actually behaves. This index loads into every session; **read a linked body only
when its hook matches your task.** To add or remove entries, see `repo-notes/README.md` (one
line here, substance in the body).

## Notes

_None yet — add the first one when a session learns something durable and non-obvious. Don't fabricate entries._
