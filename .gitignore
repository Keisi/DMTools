# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.vs/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env (config / secrets) — keep an .env.example committed for reference
.env
.env.*
!.env.example

# Local code-intelligence / tooling (machine-local, never committed)
.oby/
.spectral/
.codebridge/
.critical-review-state.json
# oby-init-generated project memory + references (regenerated locally;
# distinct from the authored root CLAUDE.md, which stays tracked)
.claude/CLAUDE.md
.claude/references/

# Personal Claude Code hooks + local settings (machine-local, never committed)
.claude/hooks/
.claude/settings.local.json
